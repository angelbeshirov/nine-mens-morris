import GameModule

/// This is the starting point in the application, here
/// we define a GameManager and choose a game type to start.
/// This is an example of console game without GUI, but it can
/// extended later on.
let gameManager = GameManager()
gameManager.startConsoleGame()
